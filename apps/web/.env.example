# Copy to `apps/web/.env` to override the API base.
# Default in nuxt.config.ts is http://localhost:8787 (local `wrangler dev`).
# For a deployed build, point this at your Worker URL.
NUXT_PUBLIC_API_BASE=http://localhost:8787
